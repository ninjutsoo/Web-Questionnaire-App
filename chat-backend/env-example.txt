# Backend Environment Variables (.env)
# Copy these settings to your .env file in the chat-backend directory

# Server Configuration
PORT=5001

# API Keys (required for local dev; production uses Firebase config: firebase functions:config:set gemini.api_key="..." sendgrid.api_key="SG....")
GEMINI_API_KEY=your_google_ai_studio_api_key_here
SENDGRID_API_KEY=your_sendgrid_api_key_here

# Firebase Admin Credentials (for production - optional)
# GOOGLE_APPLICATION_CREDENTIALS=path/to/your/firebase-admin-key.json

# NOTE: FRONTEND_URL is NO LONGER NEEDED!
# The backend uses dynamic CORS and automatically detects the request origin.
# No need to manually configure URLs or update IPs when they change.
